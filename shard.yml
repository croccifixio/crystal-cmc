name: cmc
version: 0.2.0

authors:
  - croccifixio <git@croccifix.io>

targets:
  cmc:
    main: src/cmc.cr

dependencies:
  dotenv:
    github: gdotdesign/cr-dotenv
  terminal_table:
    github: benoist/terminal_table.cr

crystal: 1.5.0

license: Unlicense
